// src/pages/OrderList.jsx

function OrderList() {
	return (
		<div>
			<h1>订单列表</h1>

			{/*
        这一节先只做页面入口
        后面我们再接订单接口：
        GET /api/orders
        POST /api/orders
      */}
			<p>这里以后显示当前用户的订单。</p>
		</div>
	)
}

export default OrderList